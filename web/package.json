{
  "name": "lunar-web",
  "private": true,
  "scripts": {
    "dev": "concurrently --names api,web --prefix-colors yellow,cyan \"cd .. && uv run uvicorn lunar_sandbox.api.app:app --reload --host 0.0.0.0 --port 8000\" \"pnpm --filter dashboard dev\"",
    "build": "pnpm --filter dashboard build",
    "codegen": "uv run python ../scripts/export_openapi.py && pnpm exec openapi-typescript packages/types/openapi.json -o packages/types/src/api.d.ts",
    "lint": "pnpm --filter dashboard lint"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "openapi-typescript": "^7.0.0",
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
